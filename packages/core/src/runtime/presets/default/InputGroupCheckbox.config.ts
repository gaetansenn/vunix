
import type { InputGroupCheckboxConfig } from '../../components/form/input/group-checkbox/InputGroupCheckbox.config';

const config = {
  slot: 'flex flex-col gap-y-2'
} as InputGroupCheckboxConfig;

export default config;