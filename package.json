{
  "name": "vunix",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "docs/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev:docs": "turbo run dev --filter=docs",
    "build:nuxt-playground": "turbo run build --filter=nuxt-playground",
    "build:vue-playground": "turbo run build --filter=vue-playground",
    "dev": "turbo run dev",
    "dev:vue-playground": "turbo run dev --filter=vue-playground",
    "dev:nuxt-playground": "turbo run dev --filter=nuxt-playground",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo run test",
    "test:vunix-core": "turbo run test --filter=vunix-core",
    "test:vunix-nuxt": "turbo run test --filter=vunix-nuxt",
    "prepare": "turbo run prepare",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.19"
}
